# AI 学习成长助手平台 — 项目答辩指南

---

## 一、项目定位与实现意图（2-3分钟）

### 1.1 一句话概述
> 一个**面向学习者**的全栈智能学习管理平台，集成 **AI 大模型对话**、**RAG 个人知识库问答**、**番茄工作法计时**、**游戏化成就系统**五大核心能力。

### 1.2 解决什么问题？
| 痛点 | 本项目的解决方式 |
|------|----------------|
| 学习资料散落各处，难以检索 | RAG 知识库：上传资料自动建立向量索引，AI 回答时自动引用 |
| 缺乏学习规划和跟踪 | 学习计划 CRUD + 多维度筛选 + 进度可视化 |
| 错题整理低效 | 错题本 CRUD + 掌握状态流转 + 分类/难度/标签筛选 |
| 缺乏持续学习动力 | 21 个预设成就 + 5 级稀有度 + 积分系统 + 实时进度反馈 |
| 专注力难以保持 | 番茄钟三阶段循环（专注/短休/长休）+ 复盘沉淀到笔记/错题 |

### 1.3 核心亮点（答辩时要强调的）
1. **RAG 检索增强生成**：用户上传的文件自动解析分块嵌入向量库，AI 对话时自动检索相关片段注入上下文，实现"基于个人知识库的问答"
2. **游戏化成就引擎**：从 6 个维度（笔记/错题/计划/资源/番茄/学习天数）实时评估 21 个成就的进度，达到目标自动永久解锁
3. **番茄钟核心算法**：使用绝对时间戳（`endAt = Date.now() + remaining`）而非递减计数器，解决 `setInterval` 漂移问题；通过 `visibilitychange` 校准后台标签页切回后的时间
4. **ECharts 按需引入**：仅加载 line/bar/pie/heatmap 四种图表类型 + 必要组件，打包体积从 ~1MB 降至 ~350KB
5. **主题联动**：通过 `provide/inject` 注入响应式主题配置，图表组件销毁重建适配深色/浅色模式

---

## 二、技术架构总览

```
┌────────────────────────────────────────────────────┐
│              前端 (Vue 3 + Vite 5)                 │
│  Element Plus  UI  │  Pinia 状态管理  │  ECharts 6 │
│  Axios (拦截器/401自动登出/全局Loading计数器)        │
│  unplugin-auto-import (自动导入Vue/Pinia/Router API) │
└──────────────────┬─────────────────────────────────┘
                   │ HTTP + SSE
                   │ /api/* → Vite Proxy → 127.0.0.1:8080
┌──────────────────┴─────────────────────────────────┐
│           后端 (Spring Boot 2.7 + JPA)              │
│  JWT 认证 (HMAC-SHA)  │  ThreadLocal 用户上下文     │
│  全局异常处理 (@RestControllerAdvice)               │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 业务 CRUD │  │ AI/RAG   │  │ 番茄/成就引擎    │  │
│  │ 7个实体   │  │ DeepSeek │  │ 纯前端核心逻辑   │  │
│  │ 9个控制器 │  │ LangChain│  │ localStorage持久化│  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
└──────────────────┬─────────────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    ▼              ▼              ▼
  MySQL 8      文件系统      DeepSeek API
  (8张表)      (资源存储)    (LLM + Embedding)
```

---

## 三、各模块技术细节讲解

### 3.1 JWT 认证与权限控制

**实现链路**：
```
WebConfig 注册拦截器 → AuthInterceptor.preHandle()
→ 提取 Authorization: Bearer <token>
→ AuthService.requireUserId(token)  // JJWT 验签 + 黑名单检查
→ AuthService.extractRole(token)    // 提取角色 claim
→ AuthContext.set(userId, role)     // 存入 ThreadLocal
→ Controller 通过 AuthContext.getCurrentUserId() / requireAdmin() 获取
→ afterCompletion() 清理 ThreadLocal
```

**讲解要点**：
- JJWT 0.12.6 使用 HMAC-SHA 签名，密钥从 `application.yml` 的 `jwt-secret` 派生
- Token 载荷包含 `sub`(userId)、`username`、`nickname`、`role`（无敏感信息）
- 登出时加入 ConcurrentHashMap 黑名单，自动清理过期记录避免内存泄漏
- 管理员权限通过 `requireAdmin()` 校验，课程/资源管理仅 admin 可操作
- **为什么用 ThreadLocal？** 避免在每个 Service 方法中传递 userId，保持代码简洁

---

### 3.2 AI 对话 + RAG 检索增强（重点模块）

**架构图**：
```
用户问题
   │
   ▼
AiService.chat(question)
   ├─ 1. AuthContext 获取 userId
   ├─ 2. RagService.buildRagPrompt(userId, question)
   │      ├─ embeddingModel.embed(question)  → 查询向量
   │      ├─ embeddingStore.search(向量, 阈值0.7)
   │      ├─ 按 userId 过滤（多租户隔离）
   │      └─ Top-3 片段拼接为"参考资料"提示词
   ├─ 3. 组装 OpenAI 兼容请求
   │      { model, messages, thinking: {enabled}, reasoning_effort: high }
   ├─ 4. WebClient POST → https://api.deepseek.com/chat/completions
   └─ 5. 解析 choices[0].message.content → 返回
```

**RAG 三段式流水线**：

| 阶段 | 技术 | 细节 |
|------|------|------|
| **摄取** | LangChain4j DocumentParser | PDF→Apache PDFBox, txt/md→TextParser, 其他→Apache Tika |
| **分割** | RecursiveDocumentSplitter | 块大小 500 字符，重叠 100 字符，保证语义连续性 |
| **嵌入** | DeepSeek text-embedding-v3 | 通过 LangChain4j 的 `OpenAiEmbeddingModel` 调用 OpenAI 兼容 API |
| **存储** | InMemoryEmbeddingStore | 内存向量库，附 userId/resourceId 元数据 |
| **检索** | 余弦相似度 | 阈值 0.7，按 userId 过滤实现多租户隔离，Top-3 |
| **增强** | 提示词拼接 | "请基于以下参考资料回答...\\n[来源: xxx]\\n内容" |

**讲解要点**：
- **为什么用 RAG？** 通用大模型不知道用户上传的个人资料，RAG 让 AI 能基于用户的知识库回答
- **为什么用 LangChain4j？** Java 生态中最成熟的 LLM 框架，内置文档解析器、分割器、嵌入存储，无需手动集成
- **多租户隔离**：检索结果按 userId 过滤，用户 A 上传的资料不会被用户 B 检索到
- **自动触发**：`ResourceService.upload()` 保存文件后自动调用 `ragService.addResourceToKnowledgeBase()`，实现"上传即索引"
- **流式 vs 非流式**：当前使用非流式（`stream: false`），可升级为 SSE 流式输出

---

### 3.3 番茄专注计时器（核心算法）

**为什么不用 `setInterval` 递减计数？**
> `setInterval` 有累积误差：JS 单线程，回调可能延迟执行，多次累积后误差可达数秒。且浏览器后台标签页会降频 `setInterval` 到 1 次/秒甚至更低。

**我们的方案：绝对时间戳**：
```javascript
// 开始
endAt.value = Date.now() + timeLeft * 1000

// 每秒 tick
tick() {
  const remaining = Math.round((endAt.value - Date.now()) / 1000)
  if (remaining <= 0) completePhase()
  else timeLeft.value = remaining
}

// 页面切回校准
document.addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'visible' && isRunning) tick()
})
```

**讲解要点**：
- 使用绝对时间戳，每次 tick 重新计算差值，无累积误差
- `visibilitychange` 事件校准后台切回时的时间
- 三阶段循环：focus(25min) → shortBreak(5min) / longBreak(15min，每4轮)
- 完成记录包含：任务关联、时长、是否完成、中断原因
- 复盘可一键沉淀到笔记（Markdown）和错题本

---

### 3.4 成就系统（游戏化引擎）

**评估流程**：
```
各 Store 数据变化
  → collectMetrics()  // 收集: 笔记数/错题数/计划数/资源数/番茄分钟/学习天数
  → achievementStore.evaluate(metrics)
  → 遍历 21 个预设成就
  → 比对 progress >= target ?
  → YES: 自动解锁 + 写入 localStorage 永久记录
  → NO: 更新 progressPercent 进度条
```

**讲解要点**：
- 21 个成就跨 4 个分类（成长/学习/任务/创作），5 种稀有度（common→legendary）
- 完全前端驱动，评估函数即时计算，无需后端请求
- `localStorage` 保存已解锁成就，一旦解锁永久点亮
- 成就数据在各页面 watch 监听变化自动重算

---

### 3.5 ECharts 按需加载 + 主题联动

**加载策略**：
```javascript
// echarts-init.js —— 仅导入实际使用的组件
import * as echarts from 'echarts/core'
import { LineChart, BarChart, PieChart, HeatmapChart } from 'echarts/charts'
import {
  CanvasRenderer, TitleComponent, TooltipComponent,
  LegendComponent, GridComponent, VisualMapComponent, CalendarComponent
} from 'echarts/components'
// 全量 ~1MB → 按需 ~350KB，减少约 65%
```

**主题联动**（ChartCard 组件）：
```javascript
// inject 获取跨级主题配置（考核 provide/inject）
const themeConfig = inject('themeConfig', { isDark: true })

// 主题切换时销毁旧图表 → 重新初始化 → 恢复数据
watch(
  () => themeConfig.value?.isDark ?? themeConfig.isDark,
  () => { disposeChart(); renderChart() }
)
```

**讲解要点**：
- `echarts-init.js` 只注册实际使用的图表和组件，Tree-shaking 移除未用代码
- `ChartCard` 通过 `inject('themeConfig')` 感知主题切换，重建图表（ECharts 主题在 init 时确定，无法中途切换）
- `ResizeObserver` 替换 `window.resize`，只监听图表容器自身尺寸变化，性能更优
- `isEmpty` 计算属性检测空数据，显示 `el-empty` 占位

---

### 3.6 前端工程化设计

| 方面 | 实现 | 讲解要点 |
|------|------|----------|
| **自动导入** | `unplugin-auto-import` + `unplugin-vue-components` | 无需手动 import Vue/Pinia API 和 Element Plus 组件 |
| **代码分割** | `manualChunks` 拆分 echarts / element-plus / vue-vendor | 首屏只加载必需代码，其他 chunk 懒加载 |
| **压缩** | gzip + brotli 双压缩（threshold: 1024 字节） | 生产环境文件体积减少 60-70% |
| **SCSS 体系** | 全局注入 variables + mixins | 统一毛玻璃卡片、渐变文字等设计语言 |
| **请求封装** | Axios 拦截器 + Loading 引用计数器 | 多个并发请求共享一个 Loading 遮罩，避免闪烁 |
| **401 处理** | 响应拦截器自动登出 + 跳转登录页 | 任何 API 返回 401 统一处理，无需在每个页面写 |

---

### 3.7 数据初始化策略

```java
@Bean
CommandLineRunner initDemoData(...) {
    return args -> {
        // 1. 先清空所有表（幂等）
        achievementRepo.deleteAll(); wrongRepo.deleteAll(); ...
        // 2. 创建 2 个用户
        // 3. 创建 5 门课程 + 52 个章节
        // 4. 创建笔记/计划/错题/资源/成就
        // 共 61 条数据，覆盖所有功能模块
    };
}
```

**讲解要点**：
- 使用 Spring `CommandLineRunner`，应用启动后自动执行
- 先清空再重建的设计保证了每次启动数据一致
- 密码明文（演示项目性质），实际部署应使用 BCrypt
- 课程 `id` 手动分配（String），章节通过 `@OneToMany(cascade=ALL)` 级联保存

---

## 四、答辩常见问题预判

**Q1: AI 回答质量如何保证？**
> 使用 DeepSeek-v4-pro 模型 + `reasoning_effort=high` + `thinking.type=enabled`，模型先进行深度推理再输出。RAG 模块提供用户个人资料作为上下文，提升回答的针对性和准确性。

**Q2: RAG 的向量数据库为什么用内存存储？**
> 当前为演示环境，`InMemoryEmbeddingStore` 足够。生产环境可替换为 Milvus/Pinecone/Chroma 等持久化向量数据库，LangChain4j 提供了对应的集成。

**Q3: 番茄钟在后台标签页还能计时吗？**
> 使用绝对时间戳方案。页面切回时通过 `visibilitychange` 事件重新计算剩余时间：`remaining = Math.round((endAt - Date.now()) / 1000)`，无论切出多久都能正确校准。

**Q4: 用户数据安全如何保证？**
> 所有 API 通过 JWT 认证。Controller 层通过 `AuthContext.getCurrentUserId()` 获取用户 ID，Service 层按 userId 过滤数据。用户 A 无法访问用户 B 的数据（包括 RAG 检索也按 userId 隔离）。

**Q5: 前端状态管理为什么选 Pinia 而不是 Vuex？**
> Pinia 是 Vue 3 官方推荐的状态管理方案，API 更简洁（Composition API 风格），完整 TypeScript 支持，无需 mutation/action 分离。本项目的所有 store 均采用 Setup Store 模式。

**Q6: 页面性能如何优化？**
> 1. 路由懒加载（`component: () => import(...)`）2. ECharts 按需引入（体积减少 65%）3. Vite 代码分割（echarts/element-plus/vue-vendor 独立 chunk）4. gzip + brotli 双压缩 5. `unplugin-auto-import` 减少手动 import 产生的冗余代码 6. 图片懒加载（IntersectionObserver）

---

## 五、答辩演示流程建议（5-8分钟）

| 时间 | 内容 |
|------|------|
| **0-1min** | 项目概述：这是什么、解决什么问题、技术栈 |
| **1-2min** | 架构讲解：前后端分离、JWT 认证流程、7 个实体关系 |
| **2-4min** | **核心亮点演示**（重点！）：AI 对话 + RAG 知识库检索 → 上传一份资料 → AI 回答自动引用 |
| **4-5min** | 番茄专注：计时器原理、复盘沉淀、图表分析 |
| **5-6min** | 成就系统：21 个成就、游戏化引擎、自动解锁 |
| **6-7min** | 其他功能快览：课程、笔记、错题、仪表盘 |
| **7-8min** | 技术总结 + 收获 + 展望 |

> **建议**：演示时用 `admin/admin123` 展示管理端，用 `zhangsan/123456` 展示用户端，两个角色的视图差异本身就是亮点。
