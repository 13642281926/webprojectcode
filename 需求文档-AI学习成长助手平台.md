# AI 学习成长助手平台 —— 需求规格说明书

> **项目名称**：AI 学习成长助手平台（AI Learning Growth Studio）
> **所属课程**：信息系统综合实训
> **技术路线**：Spring Boot 2.7 + Vue 3 + Spring Data JPA + MySQL 8.0 + Element Plus + DeepSeek AI
> **文档版本**：v2.0
> **编写日期**：2026-06-12

---

## 一、引言

### 1.1 项目背景

随着人工智能技术在教育领域的深入应用，个性化学习辅助工具已成为提升学习效率的重要手段。当前高校学生面临课程管理分散、学习计划缺乏系统跟踪、学习数据分析不足等痛点。

本项目顺应教育信息化趋势，基于 Web 全栈技术构建面向高校学生的 AI 学习成长助手平台，帮助学习者科学规划学习任务、系统管理课程资源、获取 AI 学习建议、并通过数据可视化直观了解自身学习状态。

### 1.2 项目目标

1. 构建全栈学习管理平台：Spring Boot + Vue 3 前后端分离架构，实现学习计划、课程管理、AI 智能问答、数据分析等核心功能
2. 实现角色权限管理：基于 JWT 认证 + 角色字段的权限控制（admin/user）
3. 接入真实 AI 大模型：集成 DeepSeek API + RAG 知识增强，提供智能学习建议
4. 提供数据驱动的学习洞察：集成 ECharts 图表，多维度展示学习数据
5. 符合工程化开发规范：RESTful API、MVC 分层架构、统一响应格式

### 1.3 项目范围

**范围内**：用户认证（JWT）、首页仪表盘、学习计划 CRUD、课程管理（管理员 CRUD）、AI 助手（DeepSeek + RAG）、数据分析、笔记管理、错题本、学习资源、成就系统、番茄专注、个人中心

**范围外**：移动端 App、在线支付、第三方课程平台集成

### 1.4 术语定义

| 术语 | 说明 |
|------|------|
| SPA | 单页应用（Single Page Application） |
| JWT | JSON Web Token，无状态身份认证 |
| RESTful | REST API 设计风格 |
| CRUD | 增删改查 |
| RAG | 检索增强生成（Retrieval-Augmented Generation） |
| ORM | 对象关系映射，本项目使用 Spring Data JPA |

---

## 二、系统概述

### 2.1 系统描述

AI 学习成长助手平台是基于 B/S 架构的 Web 单页应用（SPA），前端采用 Vue 3 + Element Plus，后端基于 Spring Boot 提供 RESTful API，数据层使用 MySQL 8.0 持久化存储，AI 模块集成 DeepSeek API + LangChain4j RAG。

### 2.2 用户角色

| 角色 | 权限范围 |
|------|---------|
| 管理员（`admin`） | 全部功能 + 课程增删改 |
| 普通用户（`user`） | 个人学习管理、查看课程 |

默认管理员账号：`admin` / `123456`，新注册用户默认为普通用户。

### 2.3 功能模块总览

系统共 **12 个功能模块**：

```
┌──────────────────────────────────────────────────────┐
│                AI 学习成长助手平台                     │
├────────┬────────┬────────┬────────┬─────────────────┤
│ 1.登录  │ 2.Dash- │ 3.学习  │ 4.番茄  │ 5.课程管理     │
│   认证  │  board  │   计划  │   专注  │  ★管理员CRUD   │
├────────┼────────┼────────┼────────┼─────────────────┤
│ 6.笔记  │ 7.错题  │ 8.学习  │ 9.成就  │ 10.AI 助手    │
│   管理  │   本    │   资源  │   系统  │  DeepSeek+RAG  │
├────────┼────────┼────────┼────────┼─────────────────┤
│ 11.数据 │ 12.个人 │        │        │                │
│   分析  │   中心  │        │        │                │
└────────┴────────┴────────┴────────┴─────────────────┘
```

---

## 三、功能需求

### 3.1 登录与认证模块

| 编号 | 功能 | 描述 | 业务规则 |
|------|------|------|---------|
| REQ-AUTH-001 | 用户登录 | 账号密码登录，返回 JWT Token | 账号 3-20 字符，密码 6-32 字符 |
| REQ-AUTH-002 | 用户注册 | 新用户注册 | 用户名唯一，密码一致性校验 |
| REQ-AUTH-003 | 路由守卫 | 未登录自动跳转登录页 | 保存原始路径，登录后跳回 |
| REQ-AUTH-004 | 会话持久化 | 刷新后自动恢复登录 | Token + userInfo 存储于 localStorage |
| REQ-AUTH-005 | 退出登录 | 清除 Token 和用户信息 | Token 加入内存黑名单 |

### 3.2 首页仪表盘

| 编号 | 功能 | 数据来源 |
|------|------|---------|
| REQ-DASH-001 | 统计卡片（学习天数/总时长/任务完成数） | 用户数据聚合 |
| REQ-DASH-002 | 月度学习趋势折线图 | 按 6 个月学习时长模拟 |
| REQ-DASH-003 | 每周学习分布柱状图 | 周一至周日数据 |
| REQ-DASH-004 | 任务完成率环形图 | done/doing/pending 分布 |
| REQ-DASH-005 | 快捷入口 | 跳转核心模块 |

### 3.3 学习计划模块

| 编号 | 功能 | 业务规则 |
|------|------|---------|
| REQ-PLAN-001 | 计划列表 | 支持多条件筛选（优先级/状态/关键词） |
| REQ-PLAN-002 | 新增计划 | 标题必填，默认优先级=中，默认状态=待开始 |
| REQ-PLAN-003 | 编辑计划 | 仅创建者可编辑 |
| REQ-PLAN-004 | 删除计划 | 删除前确认 |
| REQ-PLAN-005 | 状态切换 | 待开始 ↔ 进行中 ↔ 已完成 |

### 3.4 课程管理模块 ★ 管理员 CRUD

| 编号 | 功能 | 权限 | 说明 |
|------|------|------|------|
| REQ-COUR-001 | 课程浏览 | 所有用户 | 卡片网格 + 分类筛选 + 关键词搜索（400ms 防抖） |
| REQ-COUR-002 | 课程详情 | 所有用户 | 抽屉展示：封面、描述、讲师、知识点标签、章节时间轴、进度条 |
| REQ-COUR-003 | 添加课程 | **仅管理员** | 表单：ID/标题/类别/封面/描述/讲师/课时数 |
| REQ-COUR-004 | 编辑课程 | **仅管理员** | 卡片悬停显示编辑按钮 → 表单回填 |
| REQ-COUR-005 | 删除课程 | **仅管理员** | 确认对话框 → 删除 |

### 3.5 AI 助手模块

| 编号 | 功能 | 技术 |
|------|------|------|
| REQ-AI-001 | AI 对话 | DeepSeek API（deepseek-v4-pro），60s 超时 |
| REQ-AI-002 | RAG 知识增强 | 基于用户笔记/错题构建增强 Prompt |
| REQ-AI-003 | 快捷问题 | 预设考研/英语/Vue/时间管理/算法问题 |
| REQ-AI-004 | 保存为笔记 | AI 回复一键保存到笔记 |

### 3.6 数据分析模块

| 编号 | 功能 | 图表类型 |
|------|------|---------|
| REQ-ANA-001 | 月度趋势 | 折线图（6 个月学习时长） |
| REQ-ANA-002 | 周时长分布 | 柱状图（周一至周日） |
| REQ-ANA-003 | 任务分布 | 环形图（done/doing/pending） |
| REQ-ANA-004 | 课程进度 | 横向柱状图（各课程百分比） |
| REQ-ANA-005 | 学习热力图 | 日历热力图（12 周数据） |

### 3.7 其他模块

| 模块 | 核心功能 |
|------|---------|
| 番茄专注 | 25 分钟计时器 + 任务绑定 + 统计复盘 |
| 笔记管理 | 富文本编辑、分类筛选、CRUD |
| 错题本 | 题目录入、分类筛选（简单/中等/困难）、掌握标记 |
| 学习资源 | 文件上传/下载、分类浏览 |
| 成就系统 | 自动同步学习数据、解锁成就徽章 |
| 个人中心 | 资料编辑、学习统计、退出登录 |

---

## 四、非功能需求

### 4.1 性能需求

| 指标 | 目标值 |
|------|--------|
| 页面首次加载 | < 3 秒 |
| API 响应时间 | < 500ms（不含 AI 请求） |
| AI 请求超时 | 60 秒 |
| 前端构建产物 | Gzip 后 < 2.5MB |

### 4.2 安全需求

| 安全项 | 实现 |
|--------|------|
| 身份认证 | JWT Token，24h 过期 |
| 权限控制 | User.role 字段 + AuthContext.requireAdmin() |
| XSS 防护 | Vue 默认转义 |
| SQL 注入 | Spring Data JPA 参数化查询 |
| CORS | 后端 `allowedOriginPatterns("*")`（开发），生产限制域名 |

### 4.3 可用性需求

- 响应式布局（1366×768 ~ 1920×1080）
- 深色/浅色双主题一键切换
- 全局 Loading + 骨架屏
- 统一错误提示（ElMessage）
- 表单实时校验 + 中文错误信息
- 键盘操作支持（Enter 发送/ESC 关闭）

### 4.4 兼容性

| 浏览器 | 最低版本 |
|--------|---------|
| Chrome | ≥ 90 |
| Firefox | ≥ 90 |
| Edge | ≥ 90 |
| Safari | ≥ 15 |

---

## 五、数据需求

### 5.1 核心实体

系统涉及 **9 个核心实体**：用户、课程、课程章节、课程知识点、学习计划、笔记、资源、成就、错题

### 5.2 用户表数据字典

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码 |
| nickname | VARCHAR(50) | NOT NULL | 昵称 |
| avatar | VARCHAR(255) | - | 头像 URL |
| signature | VARCHAR(255) | - | 个性签名 |
| role | VARCHAR(20) | NOT NULL, DEFAULT 'user' | 角色（admin/user） |
| study_days | INT | NOT NULL, DEFAULT 0 | 累计学习天数 |
| total_hours | INT | NOT NULL, DEFAULT 0 | 累计学习小时数 |

### 5.3 课程表数据字典

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | VARCHAR(40) | PK | 课程 ID |
| title | VARCHAR(120) | NOT NULL | 课程标题 |
| category | VARCHAR(40) | NOT NULL | 分类 |
| cover | VARCHAR(255) | - | 封面 URL |
| description | VARCHAR(500) | NOT NULL | 描述 |
| progress | INT | NOT NULL, DEFAULT 0 | 进度（0-100） |
| teacher | VARCHAR(50) | NOT NULL | 讲师 |
| lessons | INT | NOT NULL | 课时数 |

完整数据字典见 `backend/src/main/resources/schema.sql`。

---

## 六、接口需求

### 6.1 统一响应格式

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 200=成功, 400=参数错误, 401=未登录, 403=无权限, 404=不存在, 500=服务器错误 |
| message | string | 提示信息 |
| data | object/array/null | 响应数据 |

### 6.2 认证方式

请求头：`Authorization: Bearer <token>`

### 6.3 API 端点清单

#### 认证
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/user/login` | 登录 |
| POST | `/api/user/register` | 注册 |
| GET | `/api/user/profile` | 获取资料 |
| PUT | `/api/user/profile` | 更新资料 |

#### 课程（★ 管理员保护）
| 方法 | 路径 | 权限 |
|------|------|------|
| GET | `/api/course/list` | 所有用户 |
| GET | `/api/course/{id}` | 所有用户 |
| POST | `/api/course` | **仅管理员** |
| PUT | `/api/course/{id}` | **仅管理员** |
| DELETE | `/api/course/{id}` | **仅管理员** |

#### AI
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/ai/chat` | DeepSeek 对话 |
| GET | `/api/ai/quick-questions` | 快捷问题 |

#### 其他
Dashboard、学习计划 CRUD、笔记 CRUD、资源 CRUD、成就同步、错题 CRUD、数据分析 — 共 47 个端点，详见各 Controller。

---

## 七、约束与假设

### 7.1 开发约束

| 约束项 | 说明 |
|--------|------|
| 技术栈 | Spring Boot + Vue 3 + Spring Data JPA + MySQL |
| 部署形式 | 本地开发环境演示 |
| 代码管理 | Git + Gitee |

### 7.2 假设条件

1. 用户具备基本计算机操作和 Web 浏览器使用经验
2. 开发环境网络可达 MySQL 8.0 和 DeepSeek API
3. AI 对话集成 DeepSeek API，需有效 API Key
4. 课程数据由管理员维护，普通用户不可增删改

---

> **文档编制说明**：本需求规格说明书基于实际项目代码编写，需求条目与前端视图组件、后端 Controller 一一对应，确保文档与代码实现一致性。
