# AI 学习成长助手平台 —— 需求规格说明书

> **项目名称**：AI 学习成长助手平台（AI Learning Assistant Platform）  
> **所属课程**：j3225706《信息系统综合实训》2025-2026-2  
> **技术路线**：Spring Boot + Vue 3 + MyBatis-Plus + MySQL + Redis + Element Plus  
> **文档版本**：v1.0  
> **编写日期**：2025-06-04  

---

## 一、引言

### 1.1 项目背景

随着人工智能技术在教育领域的深入应用，个性化学习辅助工具已成为提升学习效率的重要手段。当前高校学生面临课程管理分散、学习计划缺乏系统跟踪、学习数据分析不足等痛点。市面上虽存在各类学习工具，但缺乏将**学习计划管理、课程资源整合、AI 智能问答、数据可视化分析**融为一体的综合平台。

本项目顺应教育信息化趋势，基于 Web 全栈技术构建一款面向高校学生的 AI 学习成长助手平台，帮助学习者科学规划学习任务、系统管理课程资源、获取 AI 学习建议、并通过数据可视化直观了解自身学习状态。

### 1.2 项目目标

1. **构建全栈学习管理平台**：采用 Spring Boot + Vue 3 前后端分离架构，实现学习计划管理、课程管理、AI 智能问答、数据分析等核心功能模块。
2. **实现完善的权限管理体系**：基于 RBAC（Role-Based Access Control）模型和 JWT（JSON Web Token）认证，实现用户身份认证与细粒度权限控制。
3. **提供数据驱动的学习洞察**：集成 ECharts 可视化图表（折线图、柱状图、环形图、热力图等），多维度展示学习数据，辅助学习者自我评估与调整。
4. **符合工程化开发规范**：遵循 RESTful API 设计规范、MVC 分层架构、统一响应格式、代码规范（参考阿里巴巴 Java 开发手册），具备完整的测试策略与部署方案。

### 1.3 项目范围

**范围内**：

- 用户登录认证（JWT + BCrypt 密码加密）
- 首页仪表盘（统计卡片 + 学习趋势图表）
- 学习计划管理（CRUD + 筛选 + 分页 + 双视图模式）
- 课程管理（分类筛选 + 搜索 + 详情查看）
- AI 学习助手（智能问答 + 快捷问题）
- 数据分析（多图表联动展示学习数据）
- 个人中心（资料编辑 + 学习统计）
- 系统管理（RBAC 用户/角色/菜单权限管理）
- API 接口文档（Swagger / Knife4j）

**范围外**：

- 真实 AI 大模型推理（当前阶段使用关键词匹配模拟回复）
- 移动端原生 App
- 微信小程序端
- 在线支付功能
- 第三方平台（如慕课网、Coursera）API 集成

### 1.4 术语定义

| 术语 | 说明 |
|------|------|
| SPA | 单页应用（Single Page Application），页面无刷新切换 |
| RBAC | 基于角色的访问控制（Role-Based Access Control） |
| JWT | JSON Web Token，用于无状态身份认证 |
| RESTful | 表述性状态转移（Representational State Transfer）API 设计风格 |
| CRUD | 增删改查（Create, Read, Update, Delete） |
| MVC | Model-View-Controller，模型-视图-控制器分层架构 |
| ORM | 对象关系映射（Object-Relational Mapping），本项目使用 MyBatis-Plus |
| SPA | 单页应用（Single Page Application） |

---

## 二、系统概述

### 2.1 系统描述

AI 学习成长助手平台是一款基于 B/S 架构的 Web 单页应用（SPA），前端采用 Vue 3 + Element Plus 构建响应式用户界面，后端基于 Spring Boot 提供 RESTful API 服务，数据层使用 MySQL 进行持久化存储，Redis 提供缓存与 Token 黑名单管理。

系统核心业务流程为：用户登录认证 → 查看仪表盘概览 → 制定和管理学习计划 → 浏览课程资源 → 获取 AI 学习建议 → 查看数据分析报告 → 管理个人资料。

### 2.2 用户角色

系统设计三种用户角色，通过 RBAC 模型进行权限控制：

| 角色 | 角色编码 | 权限范围 |
|------|---------|---------|
| 系统管理员 | `admin` | 拥有所有权限，可管理系统用户、角色、菜单权限 |
| 普通用户 | `user` | 可管理个人的学习计划、查看课程、使用 AI 助手、查看个人数据分析 |
| 访客 | `guest` | 仅可查看公开课程列表（后续扩展） |

> **当前阶段**：主要实现 `admin` 和 `user` 两种角色。首次启动系统时自动创建默认管理员账号（admin / 123456）。

### 2.3 功能模块总览

系统共划分为 **8 个功能模块**（7 个业务模块 + 1 个系统管理模块）：

```
┌─────────────────────────────────────────────────────────┐
│                   AI 学习成长助手平台                      │
├──────────┬──────────┬──────────┬──────────┬─────────────┤
│ 1.登录   │ 2.Dash-  │ 3.学习   │ 4.课程   │ 5.AI 助手   │
│   认证   │  board   │   计划   │   管理   │             │
├──────────┼──────────┼──────────┼──────────┼─────────────┤
│ 6.数据   │ 7.个人   │ 8.系统   │          │             │
│   分析   │   中心   │   管理   │          │             │
└──────────┴──────────┴──────────┴──────────┴─────────────┘
```

---

## 三、功能需求

### 3.1 登录与认证模块（Login & Auth）

**需求编号**：REQ-AUTH-001 ~ REQ-AUTH-005

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-AUTH-001 | 用户登录 | 用户输入账号密码登录系统，系统验证身份后返回 JWT Token | ① 账号长度 3-20 字符 ② 密码长度 6-32 字符 ③ 密码使用 BCrypt 加密存储 ④ Token 有效期 24 小时 | `LoginView.vue` |
| REQ-AUTH-002 | 路由守卫 | 未登录用户访问需认证页面时自动跳转登录页，并保存原始路径 | ① 登录成功后重定向至原始路径 ② 已登录用户访问 /login 自动跳转 Dashboard | `router/index.js` beforeEach |
| REQ-AUTH-003 | 会话持久化 | 页面刷新后自动恢复登录状态 | ① Token 存储于 localStorage ② 应用初始化时自动加载 ③ Token 过期后清除并跳转登录页 | `stores/user.js` |
| REQ-AUTH-004 | 退出登录 | 用户主动退出，清除 Token 和用户信息 | ① 清除 localStorage ② 将 Token 加入 Redis 黑名单（TTL = 剩余有效期）③ 跳转登录页 | `ProfileView.vue` |
| REQ-AUTH-005 | Token 刷新 | Token 即将过期时自动续期 | ① 在 Token 过期前 1 小时内可刷新 ② 刷新后旧 Token 加入黑名单 | `request.js` 拦截器 |

**用例流程（登录）**：

1. 用户访问系统 → 路由守卫检测未登录 → 重定向至 `/login`
2. 用户输入账号密码 → 前端表单校验 → 调用 POST `/api/user/login`
3. 后端验证凭证 → BCrypt 比对密码 → 生成 JWT → 返回 `{ token, userInfo }`
4. 前端存储 Token 至 localStorage → Pinia 更新登录状态 → 跳转目标页面

### 3.2 首页仪表盘模块（Dashboard）

**需求编号**：REQ-DASH-001 ~ REQ-DASH-006

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-DASH-001 | 欢迎信息 | 展示用户昵称、个性签名、学习天数、累计学时 | 数据来源于用户资料和学习统计 | `DashboardView.vue`（欢迎横幅） |
| REQ-DASH-002 | 统计卡片 | 展示今日学习分钟数、学习任务总数、任务完成率（%） | ① 今日学习数据需当日实时聚合 ② 完成率 = 已完成任务 / 总任务 × 100% | `StatCard.vue` ×3 |
| REQ-DASH-003 | 学习趋势图 | 展示本周每日学习时长的折线图（带渐变面积填充） | x 轴：周一到周日；y 轴：学习分钟数 | `ChartCard.vue` + 折线图 |
| REQ-DASH-004 | 任务强度图 | 展示本周每日任务数量的柱状图（圆角柱形带渐变） | x 轴：周一到周日；y 轴：任务数量 | `ChartCard.vue` + 柱状图 |
| REQ-DASH-005 | 快捷入口 | 提供学习计划、课程管理、AI 助手、数据分析四个模块的快捷导航卡片 | 点击即跳转至对应路由 | `QuickEntry.vue` |
| REQ-DASH-006 | 最近动态 | 展示最近学习活动的时间线记录 | 按时间倒序显示最近 5 条学习记录 | Dashboard 底部分区 |

### 3.3 学习计划模块（Study Plan）

**需求编号**：REQ-PLAN-001 ~ REQ-PLAN-007

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-PLAN-001 | 计划列表 | 分页展示当前用户的学习计划列表 | ① 每页默认 10 条 ② 支持表格视图和卡片视图切换 ③ 表格含斑马纹样式 | `StudyPlanView.vue` |
| REQ-PLAN-002 | 新增计划 | 用户新建学习计划 | ① 标题必填 ② 学习内容、截止日期、优先级、状态可选 ③ 默认状态为「待开始」④ 默认优先级为「中」 | `el-dialog` 表单 |
| REQ-PLAN-003 | 编辑计划 | 用户修改已有计划的内容 | ① 仅计划创建者可编辑 ② 修改后更新时间自动更新 | `el-dialog` 表单（回填模式） |
| REQ-PLAN-004 | 删除计划 | 用户删除已有计划 | ① 删除前弹出确认对话框 ② 仅计划创建者可删除 ③ 软删除（保留记录） | `ElMessageBox.confirm` |
| REQ-PLAN-005 | 状态切换 | 点击状态标签循环切换状态 | ① 状态流转：待开始 → 进行中 → 已完成 → 待开始 | 状态标签点击事件 |
| REQ-PLAN-006 | 筛选搜索 | 按优先级、状态、关键词筛选计划 | ① 多条件可组合使用 ② 关键词搜索含 400ms 防抖 ③ 搜索匹配标题和内容 | 筛选栏 + `el-input` |
| REQ-PLAN-007 | 分页导航 | 支持分页浏览计划列表 | ① 显示总条数 ② 可切换每页条数（10/20/50）③ 显示页码跳转 | `el-pagination` |

### 3.4 课程管理模块（Course Management）

**需求编号**：REQ-COUR-001 ~ REQ-COUR-005

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-COUR-001 | 课程浏览 | 以卡片网格形式展示课程列表 | ① 卡片含封面图（懒加载）、分类标签、标题、描述、讲师、课时、进度条 ② 响应式栅格布局 | `CourseView.vue` + `CourseCard.vue` |
| REQ-COUR-002 | 分类筛选 | 按课程分类筛选 | ① 分类：前端、计算机、语言、全部 ② 使用单选按钮组切换 | `el-radio-group` |
| REQ-COUR-003 | 关键词搜索 | 按标题、描述搜索课程 | ① 400ms 防抖延迟 ② 搜索不区分大小写 | `el-input` 搜索框 |
| REQ-COUR-004 | 课程详情 | 以抽屉形式展示课程详细信息 | ① 含封面图、标题、描述、讲师、课时、进度百分比 ② 章节时间线（区分已完成/待学习）③ 知识点标签列表 | `el-drawer` + `el-timeline` |
| REQ-COUR-005 | 图片懒加载 | 课程封面图延迟加载 | ① 图片进入视口前显示占位动画 ② 基于 IntersectionObserver 实现 | `LazyImage.vue` + `v-lazy` 指令 |

### 3.5 AI 助手模块（AI Assistant）

**需求编号**：REQ-AI-001 ~ REQ-AI-004

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-AI-001 | AI 对话 | 用户输入问题，系统返回 AI 学习建议 | ① 对话界面含消息气泡（区分用户/AI）② 支持 Enter 键发送 ③ 300ms 最小加载动画 ④ 打字指示动画 | `AIChatBox.vue` |
| REQ-AI-002 | 快捷提问 | 提供预设问题标签，一键发起提问 | 预设主题：考研、英语学习、Vue 项目、时间管理、算法刷题 | 快捷标签列表 |
| REQ-AI-003 | 对话历史 | 自动保存当前会话的对话记录 | ① 对话记录按时间倒序展示 ② 页面刷新后可恢复（localStorage）③ 后端持久化存储 | AI 对话消息列表 |
| REQ-AI-004 | 关键词匹配回复 | 后端根据问题关键词匹配回复模板 | ① 匹配到关键词返回对应回复 ② 未匹配返回通用学习建议 ③ 预留真实 AI API 接入扩展点 | 后端 AiController |

> **扩展说明**：当前版本采用基于关键词匹配的模拟回复机制（Mock 层已有 5 组问答库）。设计上预留了接入真实 LLM API（如 DeepSeek、通义千问）的扩展接口，替换 `AiController` 中的回复逻辑即可升级。

### 3.6 数据分析模块（Analytics）

**需求编号**：REQ-ANA-001 ~ REQ-ANA-006

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-ANA-001 | 统计概览 | 展示累计学习小时数、完成任务数、课程完成率 | 数据跨模块聚合（学习记录 + 计划 + 课程） | `StatCard.vue` ×3 |
| REQ-ANA-002 | 月度趋势图 | 展示近 6 个月学习时长的折线图 | x 轴：月份；y 轴：累计学习小时数 | `ChartCard.vue` + 折线图 |
| REQ-ANA-003 | 周时长图 | 展示本周每日学习时长的柱状图 | x 轴：周一到周日；y 轴：学习小时数 | `ChartCard.vue` + 柱状图 |
| REQ-ANA-004 | 任务分布图 | 展示任务完成/进行中/待开始的环形比例图 | 内径 45%，外径 70%，三色分区 | `ChartCard.vue` + 环形图 |
| REQ-ANA-005 | 课程进度图 | 展示各课程学习进度的横向柱状图 | y 轴：课程名称；x 轴：进度百分比（0-100%） | `ChartCard.vue` + 柱状图 |
| REQ-ANA-006 | 学习热力图 | 以日历热力图形式展示每日学习密度 | ① 颜色深浅表示学习分钟数（0 / 1-29 / 30-59 / 60-119 / 120+）② 数据范围：近 6 个月 | `ChartCard.vue` + 热力图 |

### 3.7 个人中心模块（Profile）

**需求编号**：REQ-PRO-001 ~ REQ-PRO-004

| 编号 | 功能名称 | 功能描述 | 业务规则 | 对应前端组件 |
|------|---------|---------|---------|-------------|
| REQ-PRO-001 | 个人统计 | 展示学习天数、累计学时、账号名 | 统计数据从学习记录聚合计算 | `ProfileView.vue` 顶部卡片 |
| REQ-PRO-002 | 资料编辑 | 用户可修改昵称、个性签名、头像 URL | ① 昵称必填（2-20 字符）② 头像支持 URL 输入 ③ 保存后即时更新界面 | `el-form` |
| REQ-PRO-003 | 头像展示 | 展示用户头像图片 | ① 支持 URL 方式设置头像 ② 默认显示 DiceBear 生成的头像 | `el-avatar` |
| REQ-PRO-004 | 退出登录 | 用户点击退出登录按钮 | ① 清除 Token 和用户状态 ② 跳转至登录页 | `handleLogout` 方法 |

### 3.8 系统管理模块（System Admin）⭐ 后端新增

**需求编号**：REQ-SYS-001 ~ REQ-SYS-005

| 编号 | 功能名称 | 功能描述 | 业务规则 | 权限要求 |
|------|---------|---------|---------|---------|
| REQ-SYS-001 | 用户管理 | 查看系统用户列表，支持新增、编辑、禁用/启用用户 | ① 用户列表分页展示 ② 支持按用户名、状态筛选 ③ 新建用户默认密码 123456 | `admin` 角色 |
| REQ-SYS-002 | 角色管理 | 管理角色列表，支持新增、编辑、删除角色 | ① 角色编码唯一 ② 删除前检查是否有用户关联 | `admin` 角色 |
| REQ-SYS-003 | 菜单管理 | 管理系统菜单树，支持新增、编辑、删除菜单节点 | ① 菜单类型：目录(M)、菜单(C)、按钮(F) ② 树形结构展示 | `admin` 角色 |
| REQ-SYS-004 | 角色授权 | 为角色分配菜单/权限 | ① 树形勾选 ② 父节点选中时子节点全选 ③ 保存后清除 Redis 权限缓存 | `admin` 角色 |
| REQ-SYS-005 | 用户分配角色 | 为用户分配角色 | ① 支持多角色分配 ② 修改后清除该用户的权限缓存 | `admin` 角色 |

---

## 四、非功能需求

### 4.1 性能需求

| 指标 | 目标值 | 备注 |
|------|--------|------|
| 页面首次加载时间 | < 3 秒（P95） | 含 JS/CSS 资源加载与渲染 |
| API 接口响应时间 | < 500ms（P95） | 不含网络延迟 |
| ECharts 图表渲染 | < 1 秒 | 含数据获取 + 图表绘制 |
| 并发用户数 | ≥ 50 | 模拟 50 用户同时操作不出现明显延迟 |
| 前端构建产物体积 | Gzip 后 < 500KB | 含 ECharts 按需引入优化 |

### 4.2 安全需求

| 安全项 | 实现方式 |
|--------|---------|
| 身份认证 | JWT Token，24 小时过期，支持刷新与黑名单 |
| 密码安全 | BCrypt 加密（强度因子 10），不记录明文密码 |
| 权限控制 | RBAC 五表模型 + Spring Security @PreAuthorize 注解 |
| XSS 防护 | 输入过滤 + 输出转义，Vue 默认转义 |
| CSRF 防护 | Token 机制 + SameSite Cookie |
| SQL 注入防护 | MyBatis-Plus 默认参数化查询，禁止字符串拼接 SQL |
| 敏感数据 | 密码不返回前端，Token 不记录日志 |
| CORS 策略 | 后端配置允许前端源，生产环境限制具体域名 |

### 4.3 可用性需求

| 需求项 | 描述 |
|--------|------|
| 响应式布局 | 支持 1366×768 至 1920×1080 分辨率，栅格自适应 |
| 深色/浅色主题 | 支持一键切换双主题，含 CSS 变量体系 + Element Plus 主题联动 |
| 加载反馈 | 全局 Loading（全屏半透明遮罩）、骨架屏、进度条 |
| 错误提示 | 统一错误消息提示（ElMessage），含网络异常、401 未授权、业务错误 |
| 表单校验 | 实时校验 + 失焦校验，错误信息中文提示 |
| 无障碍 | 语义化 HTML、aria-label 标注、颜色对比度满足 WCAG AA 标准 |
| 键盘操作 | 登录表单支持 Enter 提交，对话框支持 ESC 关闭 |

### 4.4 兼容性需求

| 浏览器 | 最低版本要求 |
|--------|------------|
| Google Chrome | ≥ 90 |
| Mozilla Firefox | ≥ 90 |
| Microsoft Edge | ≥ 90 |
| Apple Safari | ≥ 15 |

### 4.5 可维护性需求

- 前端采用组件化架构，通用组件与业务组件分离
- 后端遵循 Controller → Service → Mapper 分层架构
- ESLint 代码规范检查（前端）
- Swagger / Knife4j API 自动文档生成（后端）
- 关键业务逻辑编写单元测试，覆盖率目标 ≥ 70%

---

## 五、数据需求

### 5.1 实体概述

系统涉及 **12 个核心实体**：

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   sys_user   │    │   sys_role   │    │   sys_menu   │
│   系统用户    │    │    角色      │    │   菜单权限    │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
       │ N:M           N:M │                   │
       ├───────────────────┤                   │
       │  sys_user_role    │                   │
       └───────────────────┘                   │
                               ┌───────────────┤
                               │  sys_role_menu│ N:M
                               └───────────────┘

┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  study_plan  │    │    course    │    │ai_conversation│
│   学习计划    │    │    课程      │    │  AI 对话记录  │
└──────────────┘    └──────┬───────┘    └──────────────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
    ┌─────────┴──┐  ┌──────┴──────┐  ┌─┴──────────────┐
    │course_chapter│ │course_knowledge│                 │
    │   课程章节    │ │   _point      │                 │
    └────────────┘  │   知识点       │                 │
                    └──────────────┘                 │
┌──────────────┐    ┌──────────────┐                 │
│ study_record │    │user_learning │                 │
│   学习记录    │    │   _stats     │                 │
└──────────────┘    │  学习统计     │                 │
                    └──────────────┘                 │
```

### 5.2 数据字典（核心实体）

#### sys_user（系统用户表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(255) | NOT NULL | BCrypt 加密密码 |
| nickname | VARCHAR(50) | DEFAULT '学习者' | 昵称 |
| avatar | VARCHAR(500) | - | 头像 URL |
| signature | VARCHAR(200) | - | 个性签名 |
| email | VARCHAR(100) | - | 电子邮箱 |
| phone | VARCHAR(20) | - | 手机号 |
| status | TINYINT | DEFAULT 1 | 状态（0=禁用, 1=启用） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

#### study_plan（学习计划表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| user_id | BIGINT | FK → sys_user.id, INDEX | 所属用户 |
| title | VARCHAR(100) | NOT NULL | 计划标题 |
| content | TEXT | - | 学习内容 |
| deadline | DATE | - | 截止日期 |
| priority | VARCHAR(10) | - | 优先级（high/medium/low） |
| status | VARCHAR(10) | DEFAULT 'pending' | 状态（pending/doing/done） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

#### course（课程表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| title | VARCHAR(100) | NOT NULL | 课程标题 |
| category | VARCHAR(20) | - | 分类（frontend/cs/language） |
| cover | VARCHAR(500) | - | 封面图 URL |
| description | TEXT | - | 课程描述 |
| teacher | VARCHAR(50) | - | 讲师 |
| lessons | INT | - | 总课时数 |
| progress | INT | DEFAULT 0 | 课程进度（0-100） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

#### study_record（学习记录表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| user_id | BIGINT | FK → sys_user.id, INDEX | 所属用户 |
| plan_id | BIGINT | FK → study_plan.id, NULLABLE | 关联计划 |
| duration | INT | NOT NULL | 学习时长（分钟） |
| description | VARCHAR(500) | - | 学习内容描述 |
| create_time | DATETIME | NOT NULL | 记录时间 |

其余实体（sys_role、sys_menu、sys_user_role、sys_role_menu、course_chapter、course_knowledge_point、ai_conversation、user_learning_stats）的详细字段定义见设计文档「数据库设计」章节。

---

## 六、接口需求

### 6.1 RESTful API 规范

**统一响应格式**：

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 状态码，200=成功，401=未授权，403=无权限，404=不存在，500=服务器错误 |
| message | string | 提示信息 |
| data | object/array/null | 响应数据 |

**HTTP 方法语义**：

| 方法 | 语义 |
|------|------|
| GET | 查询资源 |
| POST | 创建资源 |
| PUT | 更新资源（全量/部分） |
| DELETE | 删除资源 |

**认证方式**：请求头 `Authorization: Bearer <token>`

### 6.2 API 端点清单

#### 认证模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/user/login` | 否 | 用户登录 |
| POST | `/api/user/logout` | 是 | 退出登录 |

#### 用户模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/user/profile` | 是 | 获取当前用户资料 |
| PUT | `/api/user/profile` | 是 | 更新用户资料 |

#### 仪表盘模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/dashboard/stats` | 是 | 获取仪表盘统计数据 |

#### 学习计划模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/study-plan/list` | 是 | 分页查询计划列表（支持 priority/status/keyword 筛选） |
| POST | `/api/study-plan` | 是 | 创建学习计划 |
| PUT | `/api/study-plan/{id}` | 是 | 更新学习计划 |
| DELETE | `/api/study-plan/{id}` | 是 | 删除学习计划 |

#### 课程模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/course/list` | 是 | 查询课程列表（支持 category/keyword 筛选） |
| GET | `/api/course/{id}` | 是 | 获取课程详情（含章节和知识点） |

#### AI 助手模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/ai/chat` | 是 | 发送 AI 对话问题 |
| GET | `/api/ai/quick-questions` | 是 | 获取预设快捷问题列表 |

#### 数据分析模块

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/analytics/overview` | 是 | 获取数据分析总览数据 |

#### 系统管理模块（⭐ 新增）

| 方法 | 路径 | 认证 | 权限 | 说明 |
|------|------|------|------|------|
| GET | `/api/system/user/list` | 是 | admin | 用户列表（分页） |
| POST | `/api/system/user` | 是 | admin | 新增用户 |
| PUT | `/api/system/user/{id}` | 是 | admin | 编辑用户 |
| GET | `/api/system/role/list` | 是 | admin | 角色列表 |
| POST | `/api/system/role` | 是 | admin | 新增角色 |
| PUT | `/api/system/role/{id}` | 是 | admin | 编辑角色 |
| DELETE | `/api/system/role/{id}` | 是 | admin | 删除角色 |
| GET | `/api/system/menu/tree` | 是 | admin | 菜单权限树 |
| GET | `/api/system/role/menu/{roleId}` | 是 | admin | 获取角色已分配菜单 |
| PUT | `/api/system/role/menu` | 是 | admin | 保存角色菜单授权 |
| PUT | `/api/system/user/role` | 是 | admin | 保存用户角色分配 |

---

## 七、约束与假设

### 7.1 开发约束

| 约束项 | 说明 |
|--------|------|
| 开发周期 | 2 周（2025.6.1 - 6.12） |
| 团队规模 | 1-3 人 |
| 技术栈限制 | Spring Boot + Vue 3 + MyBatis-Plus + MySQL + Redis + Element Plus |
| 部署形式 | 本地开发环境演示 + 生产 Nginx 反向代理 |
| 代码管理 | Git + GitHub/Gitee 仓库 |

### 7.2 假设条件

1. 用户具备基本的计算机操作能力和 Web 浏览器使用经验
2. 开发环境网络可达 MySQL 和 Redis 服务
3. 生产环境具备 JDK 17+、Node.js 18+、MySQL 8.0+、Redis 7.0+ 运行环境
4. AI 对话功能当前阶段使用关键词匹配模拟，后续可替换为真实 LLM API
5. 课程和章节数据由后台管理员预先维护，不涉及用户上传课程功能

---

> **文档编制**：本需求规格说明书基于已有前端代码（`src/` 全部 7 个视图模块）逆向梳理，需求条目与前端功能组件一一对应，确保文档与代码实现的一致性。
